import { CleanWhiteSpacePipe } from './clean-white-space.pipe';

describe('CleanWhiteSpacePipe', () => {
  it('create an instance', () => {
    const pipe = new CleanWhiteSpacePipe();
    expect(pipe).toBeTruthy();
  });
});
